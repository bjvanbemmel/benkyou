export enum TextInputTypes {
  EMAIL = "email",
  TEXT = "text",
  PASSWORD = "password",
  NUMERICAL = "number",
}

export enum ButtonTypes {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export enum States {
  PROPOSED = 0,
  IN_PROGRESS = 1,
  IN_TEST = 2,
  DONE = 3,
}

export enum EditorTypes {
  FEATURE = "feature",
  REQUIREMENT = "requirement",
}
